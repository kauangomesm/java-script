// Metodos -- push(), pop(), shift(), unshit, slice(), splice()
let arr = [1, 5, 3, 10, 'alo', 'que isso']
console.log(arr)

// arr.push(false, true, 'meu filho')

// arr.pop()

// arr.shift()

// arr.unshift(1)

// arr.splice()

// arr.slice(-1)

console.log(arr[arr.length-1])
console.log(arr.slice(-1).toString())
